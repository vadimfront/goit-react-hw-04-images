import styled from '@emotion/styled';

const FormSearch = styled.form``;
const InputSearch = styled.input`
  border: none;
`;
const SearchContainer = styled.div`
  display: flex;
`;

export { FormSearch, InputSearch, SearchContainer };
